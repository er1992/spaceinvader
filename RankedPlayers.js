export default class RankedPlayer {
  constructor(userName, password, score) {
    this.userName = userName;
    this.password = password;
    this.score = score;
    // this.survivalTime = survivalTime;
    // this.mostEnemeiesDefeated = mostEnemeiesDefeated;
  }
}
